const myConstants = {
    INIT_RESPONSE: {
        status: true
    },
    FETCH_BAL_SCHEMA_ERR_RESPONSE: "Invalid request format",
    FETCH_DECISION_SCHEMA_ERR_RESPONSE: "Invalid request format",
    ACCOUNTING_PROVIDERS: ['Provider1']
}

module.exports = myConstants